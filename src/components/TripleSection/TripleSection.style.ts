import styled from '@emotion/styled'

const section = styled.div`
  width: 100%;
  min-width: 1200px;
  position: relative;
  overflow: hidden;
  background-color: white;
`

const sectionContainer = styled.div`
  width: 1040px;
  height: auto;
  margin: 0 auto;
  position: relative;
`

export default { section, sectionContainer }
